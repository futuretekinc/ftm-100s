#!/bin/sh
#
# 1. Make an entry into ucitrack for this application
# 2. Make sure the application for this gui is enabled
#
[ -n "${IPKG_INSTROOT}" ] || {
  ( . /etc/uci-defaults/luci-app-davfs2 ) && rm -f /etc/uci-defaults/luci-app-davfs2
  /etc/init.d/davfs2 enabled || /etc/init.d/davfs2 enable
}
